import { DSMConnectionConfig, DSMSession, SystemInfo, SystemUtilization, DSMProcess, FileItem, ShareLink, DockerContainer, DownloadTask, StorageVolume, PackageItem, ServiceItem, TerminalInfo, FileServiceStatus, NotificationItem, AppNotifyItem } from "./types.js";
export declare class DSMClient {
    private config;
    private rawHost;
    private rawPort;
    private rawHttps;
    private session;
    private resolvedHost;
    private resolvedPort;
    private resolvedHttps;
    getSession(): DSMSession;
    getConfig(): DSMConnectionConfig | null;
    isDemo(): boolean;
    ensureConnected(): void;
    private ensureResolvedTarget;
    setConnection(rawHost: string, rawPort: string | number, httpsFlag: boolean): void;
    private dsmRequest;
    login(config: DSMConnectionConfig, isDemo?: boolean): Promise<DSMSession>;
    logout(): void;
    postEntry(api: string, method: string, version: number, extraParams?: Record<string, string>): Promise<any>;
    getSystemInfo(): Promise<SystemInfo>;
    getUtilization(): Promise<SystemUtilization>;
    getProcesses(): Promise<DSMProcess[]>;
    listShares(): Promise<FileItem[]>;
    listFiles(folderPath: string): Promise<FileItem[]>;
    createFolder(folderPath: string, name: string): Promise<boolean>;
    deleteFile(filePath: string): Promise<boolean>;
    renameFile(filePath: string, newName: string): Promise<boolean>;
    getFileContent(filePath: string): Promise<Buffer>;
    uploadFile(folderPath: string, fileName: string, content: Buffer): Promise<boolean>;
    createShareLink(path: string, password?: string, expireDate?: string): Promise<ShareLink>;
    listShareLinks(): Promise<ShareLink[]>;
    deleteShareLink(id: string): Promise<boolean>;
    getDockerContainers(): Promise<DockerContainer[]>;
    toggleDockerContainer(id: string, action: "start" | "stop" | "restart"): Promise<boolean>;
    getDownloadTasks(): Promise<DownloadTask[]>;
    private sanitizeDsmUri;
    addDownloadTask(uri: string, destination?: string): Promise<boolean>;
    toggleDownloadTask(id: string, action: "pause" | "resume" | "delete"): Promise<boolean>;
    getStorageVolumes(): Promise<StorageVolume[]>;
    getPackages(): Promise<PackageItem[]>;
    togglePackage(id: string, action: "start" | "stop"): Promise<boolean>;
    getServices(): Promise<ServiceItem[]>;
    getService(id: string): Promise<ServiceItem | null>;
    getTerminalInfo(): Promise<TerminalInfo>;
    setTerminal(enableSsh: boolean, enableTelnet?: boolean, sshPort?: number): Promise<boolean>;
    toggleService(id: string, enabled: boolean): Promise<boolean>;
    getFileServiceStatus(): Promise<FileServiceStatus>;
    private notifyStringsCache;
    private notifyStringsExpiry;
    getNotificationStrings(): Promise<Record<string, {
        title: string;
        msg: string;
    }>>;
    private parseNotificationMessage;
    private inferCategory;
    private inferLevel;
    getNotifications(): Promise<NotificationItem[]>;
    getAppNotifications(): Promise<AppNotifyItem[]>;
    clearNotifications(): Promise<boolean>;
    markNotificationsRead(): Promise<boolean>;
    powerAction(method: "reboot" | "shutdown", force?: boolean): Promise<boolean>;
    getApiInfo(): Promise<any>;
    rawEntryCall(api: string, method: string, version: number, params?: Record<string, string>): Promise<any>;
    searchFiles(folderPath: string, pattern: string, recursive?: boolean): Promise<any>;
    getLogs(limit?: number): Promise<any>;
    execSshCommand(opts: {
        host: string;
        port?: number;
        username: string;
        password?: string;
        privateKey?: string;
        command: string;
    }): Promise<{
        success: boolean;
        stdout?: string;
        stderr?: string;
        code?: number | null;
        signal?: string;
        error?: string;
    }>;
}
export declare const dsmClient: DSMClient;
//# sourceMappingURL=client.d.ts.map